// Core Modules - these are the modules which are already created and we will use them 

setInterval(function () {
    console.log("Amir");
}, 2000);

/* var path = require('path');
var web  = "Desktop/Amir///data/a.html";
var web2  = "Desktop/Amir///data/b.html";

console.log(path.normalize(web));
console.log(path.dirname(web));
console.log(path.basename(web));
console.log(path.extname(web)); */

/* // 1. fs module - is the built in module for read and write 
var fs = require('fs');

// this creates the file - parameter 1 = name of the file, parameter2 = content inside the file 
fs.writeFileSync('a.txt', 'Hello World !!');

// for reading the file - reading as a string
console.log(fs.readFileSync("a.txt").toString()); */